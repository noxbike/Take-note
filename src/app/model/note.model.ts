export class TakeNot {
    constructor(
        public id: number,
        public title: string,
        public priority: string,
        public description: string,
        public createdDate: Date,
    ) {}
}